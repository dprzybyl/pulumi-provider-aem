name: provider-aem-native
runtime: yaml
plugins:
  providers:
    - name: aem
      path: ../../bin

resources:
  myRandomResource:
    type: aem:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
